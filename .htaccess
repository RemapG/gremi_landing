# ==============================================================================
# НАСТРОЙКИ СЕРВЕРА APACHE ДЛЯ ДЕПЛОЯ НА TIMEWEB (ШКОЛА ГР≡МИ)
# Оптимизация скорости загрузки, кэширования, безопасности и SEO
# ==============================================================================

# 1. КОДИРОВКА ПО УМОЛЧАНИЮ
AddDefaultCharset UTF-8

# 2. ПРИНУДИТЕЛЬНЫЙ HTTPS И РЕДИРЕКТ С WWW НА БЕЗ-WWW (Идеально для SEO)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Редирект с HTTP на HTTPS (SSL)
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # Редирект с www.gremischool.ru на gremischool.ru (склейка зеркал)
    RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
    RewriteRule ^(.*)$ https://%1/$1 [R=301,L]

    # Если страница не найдена (404), перенаправляем на главную (лэндинг)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.html [L]
</IfModule>

# 3. СЖАТИЕ ФАЙЛОВ ДЛЯ УСКОРЕНИЯ ЗАГРУЗКИ (GZIP Compression)
# Уменьшает вес передаваемого кода и стилей до 75%
<IfModule mod_deflate.c>
    # Включаем сжатие для основных текстовых и медиа форматов
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE image/x-icon
    AddOutputFilterByType DEFLATE image/svg+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE application/vnd.ms-fontobject
    AddOutputFilterByType DEFLATE application/x-font-ttf
    AddOutputFilterByType DEFLATE font/opentype
    AddOutputFilterByType DEFLATE font/woff
    AddOutputFilterByType DEFLATE font/woff2

    # Защита от старых багов браузеров
    BrowserMatch ^Mozilla/4 gzip-only-text/html
    BrowserMatch ^Mozilla/4\.0[678] no-gzip
    BrowserMatch \bMSIE !no-gzip !gzip-only-text/html
</IfModule>

# 4. БРАУЗЕРНОЕ КЭШИРОВАНИЕ СТАТИКИ (Leverage Browser Caching)
# Ускоряет повторные визиты пользователей в 10 раз, загружая картинки и шрифты из локальной памяти
<IfModule mod_expires.c>
    ExpiresEngine On
    ExpiresActive On

    # Кэш по умолчанию - 1 месяц
    ExpiresDefault "access plus 1 month"

    # Специфические правила для типов файлов
    ExpiresByType text/html "access plus 0 seconds"
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType text/javascript "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType application/x-javascript "access plus 1 year"
    
    # Картинки и иконки - 1 год
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresByType image/vnd.microsoft.icon "access plus 1 year"
    
    # Веб-шрифты - 1 год
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType font/opentype "access plus 1 year"
    ExpiresByType application/x-font-ttf "access plus 1 year"
    ExpiresByType application/vnd.ms-fontobject "access plus 1 year"
</IfModule>

# 5. БЕЗОПАСНОСТЬ И ЗАЩИТА СЕРВЕРА
<IfModule mod_headers.c>
    # Защита от кликджекинга (Clickjacking protection)
    Header set X-Frame-Options "SAMEORIGIN"
    
    # Защита от XSS атак (Cross-Site Scripting protection)
    Header set X-XSS-Protection "1; mode=block"
    
    # Защита типов MIME (Content-Type sniffing protection)
    Header set X-Content-Type-Options "nosniff"
</IfModule>
